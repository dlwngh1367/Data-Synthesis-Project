class RecordFormatter:
    def format(self, record):
        # Raise an error if the format() method is not implemented in subclasses
        raise NotImplementedError("Subclasses must implement the format() method.")
